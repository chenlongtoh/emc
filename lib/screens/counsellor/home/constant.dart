import 'package:flutter/material.dart';

const double CARD_HEIGHT = 70;
const double CARD_WIDTH = 200;
const EdgeInsets INNER_PADDING = const EdgeInsets.symmetric(vertical: 10, horizontal: 15);
const Radius CARD_RADIUS = Radius.circular(25);